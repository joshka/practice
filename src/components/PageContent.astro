---
import type { MarkdownPage } from '../lib/site';

type Props = {
  page: MarkdownPage;
};

const { page } = Astro.props;
---

<article class="page">
  <div class="prose" set:html={page.html} />
</article>
